---

apiVersion: v1
kind: Service
metadata:
  name: prometheus-swift-exporter-service
  namespace: {{ k8s_namespace }}
spec:
  ports:
  - name: http
    nodePort: 80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: prometheus-swift-exporter
  type: NodePort
